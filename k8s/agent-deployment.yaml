apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-service
  namespace: ai-study-assistant
  labels:
    app: agent-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-service
  template:
    metadata:
      labels:
        app: agent-service
    spec:
      imagePullSecrets:
        - name: gitlab-registry
      containers:
        - name: agent
          image: registry.gitlab.oit.duke.edu/yang.gao/ai-study-assistant/agent-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: asa-config
            - secretRef:
                name: asa-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: ai-study-assistant
spec:
  selector:
    app: agent-service
  ports:
    - name: http
      port: 8002
      targetPort: 8002
